import 'package:flutter/material.dart';

class RegisterScreem extends StatelessWidget {
  const RegisterScreem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}
